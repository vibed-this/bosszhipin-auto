[project]
name = "bosszhipin-auto"
version = "0.1.0"
description = "Boss直聘远程控制 — 油猴脚本 + Python 服务，支持 JS 远程执行"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
    "pyautogui>=0.9.54",
    "keyboard>=0.13.5",
]
